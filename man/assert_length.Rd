% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{assert_length}
\alias{assert_length}
\title{Assert that an object has a specified length or range of lengths}
\usage{
assert_length(
  x,
  exact = NULL,
  min = NULL,
  max = NULL,
  arg_name = deparse(substitute(x))
)
}
\arguments{
\item{x}{The object to check}

\item{exact}{Exact length the object should have (optional)}

\item{min}{Minimum length the object should have (optional)}

\item{max}{Maximum length the object should have (optional)}

\item{arg_name}{Optional name of the argument for more informative error messages,
defaults to the deparsed name of the object}
}
\value{
Invisibly returns the object if it passes the check, otherwise aborts
}
\description{
This function checks if an object's length matches specified constraints and
throws an error if it doesn't.
}
\examples{
x <- c(1, 2, 3)
assert_length(x, exact = 3)  # Passes
assert_length(x, min = 2)    # Passes
assert_length(x, max = 5)    # Passes
assert_length(x, min = 2, max = 5)  # Passes
\dontrun{
assert_length(x, exact = 4)  # Errors
assert_length(x, min = 4)    # Errors
assert_length(x, max = 2)    # Errors
}
}
